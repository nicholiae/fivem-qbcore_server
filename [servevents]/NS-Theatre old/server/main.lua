local QBCore = exports['qb-core']:GetCoreObject()


-- RegisterNetEvent('onResourceStart', function(resourceName)
	-- if (GetCurrentResourceName() == resourceName) then
		-- for i=1, (#Config.TVs) do
			-- local tvHash = Config.TVs[i].tvHashCode					
			-- local tvModelName = Config.TVs[i].tvModelName
			-- local spawnLocation = Config.TVs[i].tvSpawnLocation
			
			-- while not HasModelLoaded(tvModelName) do
				-- If the model isnt loaded we request the loading of the model and wait that the model is loaded
				-- RequestModel(tvModelName)

			-- end
			-- local myTv = CreateObject(tvModelName, spawnLocation.x, spawnLocation.y, spawnLocation.z, true, false, false)
			-- local roll = 0
			-- local pitch = 0
			-- local yaw = spawnLocation.w
			-- SetEntityRotation(myTv, roll, pitch, yaw, 2, true)
		-- end
	-- else
		-- return
	-- end
-- end)


